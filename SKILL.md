---
name: dads
description: "デジタル庁デザインシステム（DADS）を参照してWebサイト、Webアプリ、行政・公共系UIを設計・実装・レビューするスキル。DADS、design.digital.go.jp、公共サービスのアクセシブルなUI、HTML/CSS/JavaScript版コードスニペット、React/Tailwind CSS版コードスニペット、コンポーネント選定、基本デザイン、アクセシビリティ確認、Web実装向けスタイル方針の整理に関する作業で使う。公式MCPサーバーとFigmaは使用しない。"
---

<!-- SPDX-License-Identifier: MPL-2.0 -->

# DADS

デジタル庁デザインシステム（DADS）の公式Webサイト、Storybook、GitHubコードスニペットを参照し、アクセシビリティを優先したWeb UIを作る。
このスキルを使う環境では公式MCPサーバーとFigmaにアクセスできないため、両者を前提にした作業は扱わない。

## 基本方針

1. まず対象がページ全体、基本デザイン、個別コンポーネント、実装コード、レビューのどれかを分類する。
2. `references/site-map.md` で公式ソースと現在のサイト構成を確認する。
3. 基本デザインを決める場合は `references/foundations.md` を読む。
4. UI部品を選ぶ場合は `references/components.md` を読む。
5. HTML版またはReact版の実装へ落とす場合は `references/implementation-resources.md` を読む。
6. 完成前に `references/checklist.md` でアクセシビリティ、レスポンシブ、出典・ライセンスの確認を行う。

## 公式情報の扱い

- 仕様、使い方、アクセシビリティは `https://design.digital.go.jp/dads/` 配下を一次情報として扱う。
- コード例は公式のHTML版/React版GitHubリポジトリとStorybookを一次情報として扱う。
- 公式サイトのバージョン、更新日、提供状況は変わるため、回答、設計、レビュー、実装判断に影響する場合は該当ページを開いて最新状態を確認する。
- `references/` は2026-06-17時点の索引と実務メモであり、公式ページの代替ではない。
- MCPサーバー、Figma Community、Figmaデザインデータは使わない。公式ページにFigmaリンクがあっても、Web実装時はページ本文、Storybook、GitHubコードスニペットで代替する。
- 公式手順がFigmaデザインデータの参照を要求するスタイルガイド作成や、Figma内ガイドラインだけで確認できる事項は、このスキルではDADS公式準拠として断定しない。Web実装向けのスタイル方針整理、トークン化、既存プロジェクトへの適用方針までを対象にする。

## 実行フロー

### 1. 要件をUI構造に分解する

- コンテンツ、フォーム、ナビゲーション、フィードバック、データ表示、ページ骨格に分ける。
- 既存プロジェクトがある場合は、フレームワーク、CSS方針、既存コンポーネント、テスト方法を先に確認する。
- 既存デザインシステムやブランドガイドがある場合は、DADSをそのまま適用せず、DADSの考え方をWeb実装向けのスタイル方針やトークンへ再定義する。

### 2. 基本デザインを先に固定する

- 色、タイポグラフィ、余白、角、レイアウト、リンク、アイコン、エレベーションの順で確認する。
- 色はコントラスト要件を満たす前提で、キーカラー、共通カラー、機能カラー、アクセントカラー、セマンティックカラーを分ける。
- 画面固有の判断を行った場合は、後続のUI実装がぶれないようにプロジェクト内のスタイル定義やトークンに落とす。

### 3. コンポーネントを選ぶ

- まず `references/components.md` の用途別索引で候補を絞る。
- 候補ページでは概要、使い方、アクセシビリティ、各種リソースの順で確認する。
- ガイドライン準備中のページでは、公式Storybook/GitHubの実装を確認しつつ、一般的なHTML semanticsとWCAG/WAI-ARIA要件で暫定補完する。ただし、DADS固有ガイドラインとして未確認の事項は準拠済みと断定しない。
- 見た目だけを理由にコンポーネントを選ばず、情報構造とユーザー操作に合うものを選ぶ。

### 4. 公式コードスニペットを使う

- プレーンHTML/CSS/JavaScriptならHTML版コードスニペットを参照する。
- ReactならReact版コードスニペットを参照し、既存プロジェクトのReact/Tailwind/TypeScript構成へ合わせる。
- コードをそのまま貼り付けず、既存命名、責務分離、状態管理、テスト方針に合わせて最小差分で移植する。
- Tailwindテーマプラグインを使う場合は、対象TailwindバージョンとDADS/プラグイン対応表を公式リポジトリで再確認する。

### 5. 検証する

- キーボード操作、フォーカス順、フォーカス表示、ターゲットサイズ、コントラスト、レスポンシブ、スクリーンリーダー向けラベルを確認する。
- UI実装では可能な範囲でテスト、lint、型チェック、ビルド、ブラウザ表示確認を行う。
- 検証できない項目は、理由と残リスクを報告する。

## 参照ファイル

- `references/site-map.md`: 公式ソース、サイト構成、参照順序。
- `references/foundations.md`: 基本デザインのページ索引と要点。
- `references/components.md`: コンポーネントの用途別索引とURL。
- `references/implementation-resources.md`: HTML版/React版コードスニペット、Storybook、Tailwindプラグイン、利用条件。
- `references/checklist.md`: 実装・レビュー時の確認項目。

## 禁止事項

- 公式MCPサーバーを起動・接続・参照しない。
- Figmaデザインデータを開かない、取得しない、前提にしない。
- DADSの説明やガイドラインを、出典や更新状況を曖昧にしたまま断定しない。
- アクセシビリティ要件を見た目の都合で省略しない。
- 既存プロジェクトのUI基盤を無視してDADSコードスニペットを無加工で混ぜない。
